import { UpperPatternDirective } from './upper-pattern.directive';

describe('UpperPatternDirective', () => {
  it('should create an instance', () => {
    const directive = new UpperPatternDirective();
    expect(directive).toBeTruthy();
  });
});
